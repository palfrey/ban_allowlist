---
name: HA

on:
  push:
    branches:
    - main
  pull_request:
  schedule:
  - cron: 0 0 * * *

jobs:
  ha:
    name: Hassfest
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v7
    - uses: home-assistant/actions/hassfest@master
